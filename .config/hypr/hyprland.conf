# Set programs that you use
$te = kitty
$eo = rifle
$al = pkill wofi || wofi
$sw = [float; group barred; size 960 540] $te

$ip = 127.0.0.1
$proxy = ://$ip:7890

source = binds.conf
source = envs.conf
source = vars.conf
source = rules.conf
source = execs.conf

source = keys.conf
